{
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "express": "^4.21.1"
  },
  "scripts": {
    "start": "apt-get update && apt-get install -y minizinc && node server.js"
  }
}
